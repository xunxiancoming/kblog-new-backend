version: "3.8"

services:
  # MySQL 数据库服务
  mysql:
    image: mysql:8.0
    container_name: kblog-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: kblog
      MYSQL_USER: kblog_user
      MYSQL_PASSWORD: kblog_password
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - kblog-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10

  # Redis 缓存服务
  redis:
    image: redis:7-alpine
    container_name: kblog-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - kblog-network
    command: redis-server --appendonly yes

  # 后端 API 服务
  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    container_name: kblog-api
    restart: unless-stopped
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_started
    environment:
      - DATABASE_URL=mysql://kblog_user:kblog_password@mysql:3306/kblog
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_PASSWORD=
      - JWT_SECRET=${JWT_SECRET:-your-jwt-secret-key-change-in-production}
      - JWT_EXPIRES_IN=7d
      - PORT=3003
      - NODE_ENV=production
    ports:
      - "3003:3003"
    volumes:
      - ./api/.env:/app/.env
    networks:
      - kblog-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3003/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # 前端 Admin 服务
  admin:
    build:
      context: ./admin
      dockerfile: Dockerfile
    container_name: kblog-admin
    restart: unless-stopped
    depends_on:
      - api
    ports:
      - "80:80"
    environment:
      - REACT_APP_API_URL=http://localhost:3003
    networks:
      - kblog-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Nginx 反向代理（可选，用于生产部署）
  nginx:
    image: nginx:alpine
    container_name: kblog-nginx
    restart: unless-stopped
    depends_on:
      - api
      - admin
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./admin/build:/usr/share/nginx/html/admin
    networks:
      - kblog-network

networks:
  kblog-network:
    driver: bridge

volumes:
  mysql_data:
    driver: local
  redis_data:
    driver: local
